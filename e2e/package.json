{
  "name": "restreamer-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "setup-yt-profile": "npx playwright test youtube-studio-check --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
